name: town-wheat-oneshot-runtime-gate
'on':
  push:
    branches:
      - research/prize-solver-v0
    paths:
      - '.github/workflows/town-wheat-oneshot-runtime-gate.yml'
      - 'tools/town_wheat_oneshot_runtime_gate.py'
      - 'docs/strategy/TOWN_WHEAT_ONESHOT_RUNTIME_GATE_2026-09-18.md'
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  gate:
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install pinned runtime
        run: |
          python -m pip install -U pip
          python -m pip install 'kaggle-environments==1.32.7' 'kagglehub>=0.3.12' 'kaggle>=2.2.3' numpy
      - name: Syntax check
        run: python -m py_compile tools/town_wheat_oneshot_runtime_gate.py
      - name: Run O-TW1 autonomous runtime gate
        run: |
          mkdir -p artifacts/town-wheat-runtime
          python tools/town_wheat_oneshot_runtime_gate.py \
            --out artifacts/town-wheat-runtime/TOWN_WHEAT_ONESHOT_RUNTIME_GATE.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: town-wheat-oneshot-runtime-gate
          path: artifacts/town-wheat-runtime/*.json
          retention-days: 30
          if-no-files-found: warn
