[build-system]
requires = ["setuptools>=61", "wheel", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "kculture-kagvec"
version = "0.0.1"
description = "Kculture batched exact Kaggriculture simulator transport"
requires-python = ">=3.9"
