# V21 Stop Rule and V22 Fresh-Frontier Branch — 2026-09-21

## Status

PRE-REGISTERED while V21A workflow 35558880526 is still running and before any V21A outcome is known.

## V21 family stopping rule

The V19A-derived consensus-schedule family has exactly one remaining discovery gate: V21A semantic decomposition.

If V21A returns `V21A_SEMANTIC_WL_HEADROOM`:
- follow only the already-frozen V21B -> V21C -> V21D promotion path.

If V21A returns `V21A_SEMANTIC_NO_WL_HEADROOM`:
- close the entire V19A-derived schedule family;
- do not open V21A2/V21A3 pairwise category combinations;
- do not search arbitrary turn subsets;
- do not tune category definitions, schedule support thresholds or semantic grouping post hoc;
- do not relabel margin as the promotion target;
- do not reopen V20 state-gate tuning.

If V21A is mechanically invalid:
- repair only the mechanical defect and rerun the exact frozen V21A hypothesis;
- no strategic rule may change.

This stopping rule prevents combinatorial subset fishing on the same consumed population.

## Why the next branch must be independent

Existing binding evidence already closes the main old residual branches:

- V5: one-turn physical proposal oracle => margin-only;
- V6: 2-3-turn physical continuation => margin-only;
- V7: static ALL3 option suppression => margin-only;
- V8: residual SELL ordering => narrow/unsafe local headroom;
- V9: one-turn residual structural V48 imitation => no W/L headroom;
- V10: cumulative residual V48 market imitation => closed;
- V18B/V18B2: teacher-residual controller distillation => not robustly distillable;
- V19C/V20A: frozen consensus schedule => regime-dependent, then no W/L-positive training labels on new discovery population.

Therefore failure of V21A will mean the current frontier evidence has been mined far enough that another local mutation of the same schedule/population is not justified.

## V22 independent branch — fresh current-frontier refresh

Activate V22 only if V21A closes without semantic W/L headroom.

Purpose:
refresh the competition frontier and discover whether the present ALL3 residual losses are still generated by the same mechanisms/population.

V22 is not a continuation of V19A and must not use the V19A schedule as a candidate.

### V22A — current executable frontier refresh

Frozen intent:
- query the current public Kaggriculture frontier at execution time;
- resolve exact executable public package versions;
- build a source-SHA-deduplicated frontier pool;
- exclude exact ALL3/V47 self copies;
- no opponent identity may become a runtime feature.

Discovery evaluation:
- exact ALL3 only;
- fresh seeds reserved for V22 and not used in V19-V21;
- both seats;
- record score/margin and public legal trajectory features.

Initial frozen seed block:
- 79101..79106.

Population target:
- up to 12 unique executable frontier source SHAs;
- prioritize current ranking only for offline sampling, never runtime logic.

READY requires:
- >=8 unique executable frontier source SHAs;
- >=96 mechanically valid ALL3 contexts, unless fewer sources make the exact combinatorial maximum smaller;
- zero source/replay failures after bounded acquisition retry;
- >=12 ALL3 non-win contexts;
- non-wins span >=4 unique source SHAs;
- non-wins span >=3 seeds.

If READY:
`V22A_FRESH_FRONTIER_HARD_POPULATION_READY`.

If insufficient residual difficulty:
`V22A_FRESH_FRONTIER_TOO_EASY_REFRESH_LATER`.

If acquisition/mechanics fail:
`V22A_MECHANICS_INVALID`.

### V22B — fresh domain upper bound

Dormant until V22A READY.

On the frozen V22A non-win population, compare pre-registered domain modes:
- BASE;
- MARKET_ONLY;
- PHYSICAL_ONLY;
- FULL_SHADOW.

The shadow source for a context is the exact current frontier package already used as that context's opponent; it is offline upper-bound evidence only and may not be copied into production.

Interpretation:
- MARKET_ONLY W/L headroom => open a new market-mechanism discovery branch on the refreshed population;
- PHYSICAL_ONLY W/L headroom => physical domain may be reopened only because the population changed materially;
- FULL only without domain-specific headroom => diagnose interaction, do not deploy shadow logic;
- no domain W/L headroom => stop local option mining on ALL3 and reassess baseline architecture / hosted strategy.

No Kaggle submission is authorized by V22A or V22B.

## Final project-level stopping signal

If:
1. V21A closes the schedule family, and
2. V22A produces a sufficiently hard fresh population, but
3. V22B shows no W/L upper-bound headroom in MARKET_ONLY or PHYSICAL_ONLY,

then do not continue generating CR/V-number micro-experiments around ALL3.

At that point the evidence supports an architectural reset or competition-strategy decision rather than more local tuning.
