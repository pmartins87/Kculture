# ALL3 V21C Semantic Candidate Package/Parity Protocol — 2026-09-21

## Status

DORMANT / PRE-REGISTERED BEFORE V21A OUTCOMES.

Activate only if:
1. V21A = `V21A_SEMANTIC_WL_HEADROOM`; and
2. V21B = `V21B_SEMANTIC_FRESH_PASS`.

## Candidate identity

The packaged candidate must be exactly:

- frozen ALL3 host;
- frozen V19A schedule SHA256 `c68d857500b71a40f8cf4ef5f2ff693f6da1d97ced03e7d7f19eaedfded1ff22`;
- exact single semantic mode selected by the binding V21A artifact;
- exact semantic-category function used by V21A/V21B;
- exact O-TM1 market projection semantics;
- no further threshold, turn, category, product, quantity or rule change.

Any mutation after V21A/V21B invalidates promotion.

## Deterministic package

Package name:
`KCULTURE_V47_ALL3_SM1_V1.tar.gz`.

The package must be self-contained for Kaggle hosted execution and must not depend on external V19A/V21A artifacts at runtime.

Required receipt:
- archive SHA256;
- candidate `main.py` SHA256;
- selected semantic mode;
- embedded schedule SHA256;
- entrypoint;
- source commit.

## Fresh parity population

Frozen parity seeds:
- 79001
- 79002

Frozen opponents:
- exact V47 mirror;
- exact current V48 reference;
- exact tactical-memory reference.

Both seats.

Expected:
- 2 seeds × 3 opponents × 2 seats = **12 paired package-vs-library parity contexts**.

## PASS

`V21C_PACKAGE_PARITY_PASS` requires:
- deterministic package build;
- 12/12 action-trace parity;
- 12/12 terminal reward parity;
- exact selected mode parity;
- exact schedule SHA parity;
- no credential/runtime dependency;
- zero failures.

Otherwise:
`V21C_PACKAGE_PARITY_FAIL_CLOSE`.

Only PASS may activate V21D.

No Kaggle submission is authorized by V21C alone.
