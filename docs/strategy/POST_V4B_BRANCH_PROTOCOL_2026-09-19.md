# Post-V4B Branch Protocol — 2026-09-19

This protocol is frozen before the V4B result.

## If V4B shows W/L headroom

Do not deploy or distill full-episode V48 market imitation.

Open a temporal localization diagnostic on the **same V4B discovery seeds** using these
predeclared market-only windows:

- W0: steps 0–215
- W1: 216–335
- W2: 336–431
- W3: 432–527
- W4: 528–623
- W5: 624–718

For every V4B context that improves W/L, test:
1. each single window independently;
2. cumulative suffixes W5, W4+, W3+, W2+, W1+.

Exact V47 farmer/hands remain executed at all times. V48 is only an offline market proposal source.

Localization objective:
- smallest predeclared window/suffix that reproduces the W/L gain;
- ties break by fewer substituted turns, then later start step.

After localization:
- inspect recurring action transformations inside the minimal winning interval;
- rewrite them as a first-party legal-state option;
- validate on fresh seeds and broad regression population before option-library admission.

No selector training before a first-party causal option exists.

## If V4B shows no W/L headroom

Inspect `physical_fallback_turns`.

### Low physical fallback

Close the hypothesis that V48 market imitation alone can rescue V47 under fixed V47 physical policy.
Do not extend the market horizon further.

Reopen mechanism search outside simple imitation:
- terminal handling;
- funding/storage guards;
- market action generation from state rather than V48 imitation;
- or physical programme interaction only if new causal evidence appears.

### Material physical fallback

Activate dormant V4C:
- `tools/v48_state_basin_coupling_v4c.py`;
- fresh seeds 74201..74204;
- measure when market substitutions induce later V47/V48 physical divergence.

If V4C confirms near-term coupling, the next search target is a **joint state-transition guard**,
not market-only imitation and not arbitrary physical replacement.

## Hosted policy

No branch in this protocol authorizes a Kaggle submission by itself.
