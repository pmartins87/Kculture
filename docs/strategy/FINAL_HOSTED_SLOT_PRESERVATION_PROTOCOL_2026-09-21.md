# FINAL HOSTED SLOT PRESERVATION PROTOCOL — 2026-09-21

## Status

PRE-REGISTERED while V27A is still running and before any V27 candidate exists.

This protocol protects the final Kaggriculture submission slots from late-stage experimental churn.

## Current hosted pair

- O-RW1 submission: `56336027`;
- ALL3 submission: `56367770`.

Latest observed project snapshots:
- ALL3 rating: **2412.1**;
- O-RW1 rating: **2361.6**.

These ratings come from different live histories and are not a causal A/B estimate.

CR053_REAL `56073870` remains a legacy historical anchor (~2064.8), not the current strongest hosted result.

## Protected fallback

**ALL3 is the protected final-slot fallback.**

No new research candidate may replace ALL3 merely because it is novel, locally interesting, or margin-positive.

## Challenger slot

Until a new candidate proves otherwise, O-RW1 is the challenger/replacement slot.

A genuinely new candidate may replace O-RW1 only after all of:

1. exact local mechanical parity;
2. no runtime source/opponent identity;
3. frozen fresh multi-source/multi-seed W/L gate PASS;
4. no major catastrophe edge;
5. hosted-package reproducibility PASS;
6. explicit final submission authorization.

## Final evaluation constraint

Kaggriculture tracks only the latest two submissions for final evaluation.

Therefore every later hosted submission potentially changes the final pair.

No exploratory submission is allowed after this protocol unless its role in the two-slot final pair is explicitly understood.

## Default final pair if no new candidate qualifies

- ALL3 `56367770`;
- O-RW1 `56336027`.

Do not resubmit CR053 simply to restore a historical rating snapshot.

## If one new candidate qualifies

Submit it only with the intent to replace O-RW1, while preserving ALL3 as the other final slot.

## If two independent new candidates qualify

Do not submit both automatically.

Compare them under one frozen offline fresh gate and choose at most one challenger unless there is explicit evidence that replacing ALL3 is justified.

## Deadline

Final Kaggriculture submission deadline:
**2026-09-30 23:59 UTC**.

The project should enter final-slot protection mode before the deadline rather than consume the last submissions with unvalidated experiments.

## Automatic submission

Forbidden.

A hosted submission that changes the final two-slot set requires explicit authorization.
