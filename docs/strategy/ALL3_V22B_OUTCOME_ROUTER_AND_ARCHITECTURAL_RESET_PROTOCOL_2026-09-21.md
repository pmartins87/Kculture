# ALL3 V22B Outcome Router and Architectural Reset Protocol — 2026-09-21

## Status

PRE-REGISTERED while binding V22B workflow `35566168354` is still running and before any V22B treatment outcome is available.

This document does not change the V22B gate. It determines what happens after each already-frozen V22B decision.

## Inputs

Binding V22B modes:
- BASE;
- MARKET_ONLY;
- PHYSICAL_ONLY;
- FULL_SHADOW.

Binding V22B decision labels remain exactly those defined in:
`docs/strategy/ALL3_V22B_FRESH_FRONTIER_DOMAIN_UPPER_BOUND_PROTOCOL_2026-09-21.md`.

Observed V22A BASE functional clusters are frozen by:
`docs/strategy/ALL3_V22B_FUNCTIONAL_DIVERSITY_INTERPRETATION_2026-09-21.md`.

## Router

### 1. V22B_MARKET_DOMAIN_HEADROOM

Open exactly one new market-mechanism discovery family.

Do not reopen:
- V19A schedule subsets;
- V20 classifiers;
- V21 semantic category combinations;
- old V14/V18 threshold tuning.

The new family must be derived from recurrent legal state/action structure in the refreshed V22A population, not opponent identity.

### 2. V22B_PHYSICAL_DOMAIN_HEADROOM

Open exactly one new physical-mechanism discovery family.

The old V5-V7 physical branch remains historically closed; reopening is justified only because the V22A frontier population is materially refreshed.

No old physical threshold/composition search is resumed automatically.

### 3. V22B_BOTH_DOMAINS_HEADROOM

Only one mechanism family may be opened first.

Select MARKET versus PHYSICAL deterministically using the following ordered criteria:

1. greater number of improved V22A BASE functional outcome clusters;
2. greater number of improved-score contexts;
3. greater number of improved seeds;
4. greater number of improved source SHAs;
5. greater mean score delta;
6. greater mean margin delta;
7. if still exactly tied, choose MARKET lexically as the frozen final tie-break.

The losing domain remains dormant and may be revisited only if the selected domain later fails its fresh causal gate.

No simultaneous two-domain search.

### 4. V22B_CROSS_DOMAIN_INTERACTION_HEADROOM

Do **not** create a MARKET-only or PHYSICAL-only option.

This outcome means recoverable headroom appears only when market and physical behavior move together.

Trigger an architectural interaction reset:
- stop additive one-domain ALL3 option mining;
- identify the earliest recurrent coupled market+physical divergences shared across multiple functional source clusters;
- build at most one interaction-controller hypothesis;
- require a fresh causal gate before any hosted packaging.

FULL_SHADOW itself is never deployable.

### 5. V22B_NO_DOMAIN_WL_HEADROOM_RESET

Stop local option mining around exact V47 + O-RW1 + O-TW1 + O-LQ2.

Do not create V23 micro-patches.

Trigger a baseline-architecture reassessment with three allowed questions only:

A. **Host architecture:** is the additive option-host design itself preventing long-horizon coordinated behavior?

B. **Policy baseline:** should exact V47 cease to be the immutable base and be replaced by a materially different first-party architecture?

C. **Competition strategy:** does current public-frontier evidence justify spending remaining effort on a new architecture versus preserving the best proven hosted candidate / publication track?

The reset must begin with a fresh architecture-level benchmark/protocol, not local threshold tuning.

### 6. V22B_MECHANICS_INVALID

Repair mechanics only and rerun the exact frozen V22B:
- same 92 contexts;
- same source SHAs;
- same seeds/seats;
- same four modes;
- same gate.

No strategic mutation is allowed.

## Promotion discipline after a domain pass

A V22B domain pass is upper-bound evidence, not deployable policy evidence.

Before option-library admission, any derived first-party mechanism must:
- be opponent-identity-free;
- use only legal runtime state;
- be compact/pre-registered;
- pass a causal test on the V22A-derived discovery population;
- then pass untouched fresh validation.

If V22B source-SHA robustness is confined to one frozen functional outcome cluster, fresh validation must span at least two distinct functional sources before promotion.

## Kaggle

No V22B decision authorizes a Kaggle submission.
