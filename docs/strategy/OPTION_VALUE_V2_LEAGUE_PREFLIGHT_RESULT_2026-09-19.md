# Option-Value V2 Diverse League Preflight — PASS — 2026-09-19

Workflow `35421184517`.

Seven public opponent packages were acquired from Kaggle output packages, hash-verified,
and loaded with the official hosted entrypoint contract.

Validated league:
1. `v47_mirror` — modern41 V47 — entrypoint `_y_agent_shopherd`;
2. `ready_stock` — modern41 ready-stock — entrypoint `agent`;
3. `v48` — modern41 queue cleanup — entrypoint `_e335_agent`;
4. `router_2715` — four-program router — entrypoint `agent`;
5. `conditional_memory` — conditional-memory literal programme — entrypoint `_kaggle_submission_entrypoint`;
6. `tactical_memory` — tactical-memory literal programme — entrypoint `_kaggle_submission_entrypoint`;
7. `best_market` — literal market-strategy family — entrypoint `kaggriculture_e283_agent`.

All observed `main.py` SHA-256 values exactly matched the frozen Top-30 audit identities.
Failures: **0**.

V39 and V38 legacy13 notebooks were tested first but are no longer retrievable through the
current Kaggle CLI path. They were not silently substituted by unknown bytes. The final
league uses the independently structured `2715.6` multi-program router instead.

Binding verdict:
**OPTION_VALUE_V2_LEAGUE_PREFLIGHT_PASS**.

No Kaggle submission is authorized.
