# CR091 — run 1 invalid execution record

Date: 2026-09-15  
Authoritative branch: `fix/kaggle-parity-v1`

## Status

Workflow run `34987640694` is **INVALID FOR STRATEGIC INFERENCE**. It does not pass or fail the CR091 latent-priority hypothesis.

The frozen CR091 protocol itself is unchanged: `docs/strategy/CR091_HIERARCHICAL_MARKET_OPTION_PROTOCOL_2026-09-15.md`.

## What failed

The first CR091 harness read `main.py` from each historical package and executed it directly with Python `exec()`. That was not package-faithful execution. The exact CR052 package requires the hosted-style package context and therefore failed all 32 CR052 treatment episodes.

Package receipts themselves were correct:

- CR052_REAL SHA `b650a31d091323f2510aede0265937d3193a82a99109eadc8ab39c6e85db278d`
- CR053_REAL SHA `095080e791bd8d58369893c1a421beb57b7f64c2808c011f576e09684ffb9a15`
- CR083 SHA `648fbcdb370f7e48fafda18a1112c5f1b57a17a81b7191f010fe4058f41a41b8`
- CR086 SHA `11296a4e658f37c109a2cc953be0ea7db8e2fbde6286ac483e0466f52f4af888`

Because one frozen opponent edge had zero valid games, the gate is incomplete and no promotion criterion may be evaluated from this run.

## Non-binding partial observation

The other 96 episodes were mechanically clean:

- physical parity `69040/69040`
- market-order multiset parity `69040/69040`
- base exact parity `34520/34520`
- zero parity violations
- latent option active with `864` reorder events

On those three executable edges only:

- vs CR053_REAL: base `0.5000` -> latent `0.6875`, delta `+0.1875`
- vs CR083: `0.5000` -> `0.5000`
- vs CR086: `0.5000` -> `0.5000`

These numbers are **non-binding diagnostics only**. They must not be called a CR091 PASS because CR052 was absent.

## Repair

The rerun changes execution semantics only. It keeps unchanged:

- treatments and exact option logic
- package SHAs
- opponents
- seeds `91301..91308`
- both seats
- all W/L promotion thresholds
- no hosted submission
- sealed final holdout

The repaired harness uses the already validated `kaggle_exact_runtime.AgentProcess` path:

- fresh spawned process per package per episode
- official `kaggle_environments.agent.Agent` wrapper
- `Environment.__get_shared_state(seat).observation`
- official reference stepping/overage accounting
- no falsy-action PASS substitution
- exact CR053 package is also the candidate backbone source; the CR086 option is applied to the action returned by that package

Repair commit on `research/first-principles-economy-v1`: `f49bff2e7adde626b01b9fa3c0413f7fc97d9649`.

Binding rerun: **`34990757344`**.

Only that rerun may produce the CR091 strategic verdict.
